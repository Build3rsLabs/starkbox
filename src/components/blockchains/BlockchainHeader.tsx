import GradientHeader from '../common/GradientHeader';

const BlockchainHeader = () => {
  return (
    <GradientHeader
      title="Cosmos Blockchains"
      description="Explore projects and opportunities across the Cosmos ecosystem"
      align="center"
    />
  );
};

export default BlockchainHeader;